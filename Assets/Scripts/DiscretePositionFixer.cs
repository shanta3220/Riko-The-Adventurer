using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DiscretePositionFixer : MonoBehaviour {
     /* public float pixPerUnit = 40;
    void LateUpdate() {
        transform.position = new Vector3(
            Mathf.Round(transform.parent.position.x * pixPerUnit) / pixPerUnit,
            Mathf.Round(transform.parent.position.y * pixPerUnit) / pixPerUnit,
            transform.parent.position.z);
    }

  public Vector3 PixelPerfectClamp(Vector2 moveVector, float pixelPerUnit) {
        Vector3 vectorInPixels = new Vector3(
           Mathf.Round(moveVector.x * pixelPerUnit) / pixelPerUnit,
           Mathf.Round(moveVector.y * pixelPerUnit) / pixelPerUnit, transform.parent.position.z);
        return vectorInPixels;
    }*/
}